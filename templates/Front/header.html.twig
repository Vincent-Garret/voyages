{% extends  "base.html.twig" %}

{% block header %}

<header class="container-fluid">

    {% if is_granted("ROLE_USER") %}
        <ul class="granted row justify-content-between align-items-center">
            <li><a href="{{ path('home') }}">Voyages</a></li>
            <li><a href="{{ path('trips') }}">Mon Profil</a></li>
            <li><a href="{{ path('userList') }}">Chercher un Voyageur</a></li>
            <li><a href="{{ path('app_logout') }}">Se déconnecter</a></li>   
        </ul>
            {% else %}
        <ul class="notGranted row justify-content-between align-items-center">
            <li><a href="{{ path('home') }}">Voyages</a></li>
            <li><a href="{{ path('app_login') }}">Se connecter</a></li>
            <li><a href="{{ path('register') }}">S'inscrire</a></li>
        </ul>
    {% endif %}

</header>
{% endblock %}
